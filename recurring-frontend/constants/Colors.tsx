type StatusColors = {
  [key: string]: string;
};

export const COLORS: StatusColors = {
  planning: `bg-orange-500`,
  active: `bg-blue-700`,
  completed: `bg-green-700`,
  backlog: `bg-gray-700`,
};
